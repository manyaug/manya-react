import React from 'react';

/**
 * OnlineGate Component (Stub)
 * Blocks access to children if offline.
 */
export const OnlineGate = ({ children }) => {
    // Basic stub, just renders children for now
    return <>{children}</>;
};
